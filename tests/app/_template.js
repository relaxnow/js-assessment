'use strict';
define([ 'use!underscore' ], function (_) {
  describe("", function () {
    var fn;

    beforeEach(function () {
      fn = function() {};
    });

    it("", function () {
      // Redefine fn here

      expect(fn()).to.be();
    });
  });
});